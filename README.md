## 서버 실행

먼저 서버를 실행합니다.

```bash
./server 9000
```
## 클라이언트 실행
새 터미널을 열고 클라이언트를 실행합니다.

```bash
./client 127.0.0.1 9000
```

여러 사용자의 동시 채팅을 시험하려면 클라이언트 터미널을 두 개 이상 실행합니다.

서버를 종료할 때는 서버 터미널에서 `Ctrl+C`를 입력합니다.
